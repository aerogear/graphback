 {{#each types}}
{{! TODO disable when no pagination enabled}}
type {{name}}Page {
    {{lowercase name}}: [{{name}}!]!
    nextToken: String
}
{{/each}}

type Query {
{{#each resolvers.query}}
    {{schemaDefinition}}
{{/each}}
}
