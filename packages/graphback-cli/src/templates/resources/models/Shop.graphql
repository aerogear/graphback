type Shop {
  id: ID!
  name: String!
  ## Relationship
  products: [Product]!
  customers: [Person]!
}

type Person {
  id: ID!
  name: String!
}

type Product {
  id: ID!
  name: String!
  quantity: Int!
}