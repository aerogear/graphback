type Shop {
  id: ID!
  name: String!
  ## Relationship
  products: [Product]!
  customers: [Person]!
  owner: Person!
}

type Person {
  id: ID!
  name: String!
}

type Product {
  id: ID!
  name: String!
  quantity: Int!
}