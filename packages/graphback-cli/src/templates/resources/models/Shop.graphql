""" @model """
type Shop {
  id: ID!
  name: String!
  ## Relationship
  """
  @oneToMany(field: 'shops')
  """
  products: [Product]!
  """
  @oneToMany(field: 'shops')
  """
  customers: [Person]!
  """
  @oneToOne
  """
  owner: Person!
}

""" @model """
type Person {
  id: ID!
  name: String!
}

""" @model """
type Product {
  id: ID!
  name: String!
  quantity: Int!
}
