type Shop {
  id: ID!
  name: String!
  ## Relationship
  products: [Product!]!
  customers: [Customer!]!
}

type Customer {
  id: ID!
  name: String!
}

type Product {
  id: ID!
  name: String!
  quantity: Int!
}