
""" @model """
type Note {
  id: ID!
  title: String!
  description: String!
  ## Relationship
  """
  @oneToMany(field: 'note')
  """
  comment: [Comment]!
}

""" @model """
type Comment {
  id: ID!
  title: String!
  description: String!
}
