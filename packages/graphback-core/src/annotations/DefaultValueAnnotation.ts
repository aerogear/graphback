/**
 * An interface that represents the parsed metadata from the `@default(value: <metadata-value>)` annotation.
 *
 */
export interface DefaultValueAnnotation {
  /**
   * The value held by this metadata.
   */
  value: any;
}
