{{!Declare top level interface }}
interface Node {
    id: ID!
}

{{!TODO enable complex queries }}
{{#each types}}
input {{name}}Filter {
    {{#each fields}}
    {{ name }}: {{type}}
    {{/each}}
}
{{/each}}