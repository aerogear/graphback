{{#each types}}
{{! TODO disable when no pagination enabled}}
type {{name}}Pagination {
    items: [{{name}}]!
    pageInfo: PaginationInfo!
}
{{/each}}

type Query {
{{#each resolvers.query}}
    {{schemaDefinition}}
{{/each}}
}
