

/**
 * Loggger component that provides audit log information
 * TODO - This is just an example component that can be used in service layer
 */
export const logger = (message: string) => {
    // tslint:disable-next-line: no-console
    console.log(message);
};