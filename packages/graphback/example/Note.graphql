type Note {
  id: ID!
  title: String!
  description: String!
  ## Relationship
  comment: [Comment]! @OnetoMany(field: "noteComment")

}

type Comment @Model(paginate: false){
  id: ID!
  title: String!
  description: String!
}