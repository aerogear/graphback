type Note @config(paginate: true){
  id: ID!
  title: String!
  description: String!
  ## Relationship
  comment: [Comment]! @OnetoMany(field: "noteComment")

}

type Comment {
  id: ID!
  title: String!
  description: String!
}