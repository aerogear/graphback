""" @model """
type Note {
  id: ID!
  title: String!
  description: String
  """
  @oneToMany(field: 'note')
  """
  comments: [Comment]!
  tasks: [Task]!
}

""" @model """
type Comment {
  id: ID!
  text: String
  description: String
  """
  @oneToOne
  """
  metadata: CommentMetadata
}

"""
@model
"""
type CommentMetadata {
  id: ID!
  opened: Boolean
}

type Task {
  title: String
}

type Query {
  helloWorld: String
}
