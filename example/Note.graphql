type Note {
  id: ID!
  title: String!
  description: String!
  ## Relationship
  comment: [Comment]! @OneToMany(field: "comments_note")
}

type Comment {
  id: ID!
  title: String!
  description: String!
}
