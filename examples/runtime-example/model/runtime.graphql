
## Model that will be used at server runtime

type User {
    id: ID!
    name: String
    age: String
    date: String
}

type City {
    id: ID!
    name: String
}

type Note {
    id: ID!
    text: String
}
