type User {
  id: ID!
  name: String!
  """
  @db.oneToMany: 'inUser'
  """
  inTransactions: [Transaction!]!
  """
  @db.oneToMany: 'outUser'
  """
  outTransactions: [Transaction!]!
}

type Transaction {
  id: ID!
  amount: Int!
  inUser: User!
  outUser: User!
}