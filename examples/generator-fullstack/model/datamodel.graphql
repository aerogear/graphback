"""
@model
"""
type Note {
  id: ID!
  title: String!

  description: String
  """
  @db.oneToMany: 'commentNote'
  """
  comments: [Comment]!
  likes: Int
}

"""
@model
"""
type Comment {
  id: ID!
  text: String
  author: String
}

type Query {
  mostLikedNote: Note!
}

type Mutation {
  likeNote: Note!
}
