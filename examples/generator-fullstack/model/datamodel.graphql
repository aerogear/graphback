type Note {
  id: ID!
  title: String!
  description: String!
  ## Relationship
  comments: [Comment!]! @OneToMany(field: "ownerId")
}

type Comment {
  id: ID!
  title: String!
  description: String!
  owner: Note
}
