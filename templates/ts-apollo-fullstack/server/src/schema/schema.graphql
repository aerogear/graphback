## NOTE: This schema was generated by Graphback and should not be changed manually



""" @model """
type Todo {
  id: ID!
  title: String
  description: String
  version: Int!
  completed: Boolean
}

input TodoInput {
  id: ID
  title: String
  description: String
  version: Int
  completed: Boolean
}

type Query {
  findAllTodos: [Todo]!
  findTodos(fields: TodoInput): [Todo]!
} 

type Mutation {
  createTodo(input: TodoInput): Todo!
  updateTodo(input: TodoInput): Todo!
  deleteTodo(input: TodoInput): Todo!
} 

type Subscription {
  newTodo(input: TodoInput): Todo!
  updatedTodo(input: TodoInput): Todo!
  deletedTodo(input: TodoInput): Todo!
} 

