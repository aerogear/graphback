""" @model """
type Note {
  id: ID!
  title: String!
  description: String
  """
  @db(type: 'json')
  """
  comment: Comment
}

type Comment {
  id: ID!
  text: String!
  description: String!
  test: Test!
  test2: [Test!]!
}

type Test {
  hello: String
}
